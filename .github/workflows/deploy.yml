name: Refresh data and deploy
on:
  push:
    branches: [main]
  schedule:
    - cron: "17 6 * * *"   # daily, 06:17 UTC (the bulletin is updated weekly)
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pandas openpyxl
      - run: |
          mkdir -p site/data
          cp web/index.html site/index.html
          cp web/cz_regions.json site/data/cz_regions.json
          python scripts/fetch_data.py
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
